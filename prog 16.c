#include <stdio.h>

int main() {
    int a, b, c;
    int product;

    a = b = c = 3;  

    product = a * b * c;

    printf("Values: a = %d, b = %d, c = %d\n", a, b, c);
    printf("Product = %d\n", product);

    return 0;
}
