#include <stdio.h>

int main() {
    int n;

    // Input value of n
    printf("Enter the value of n: ");
    scanf("%d", &n);

    // Check if n is greater than 100
    if (n > 100) {
        printf("n is greater than 100.\n");
    }

    return 0;
}
